// Use DBML to define your database structure
// Docs: https://dbml.dbdiagram.io/docs

Table flight {
  flightId var [primary key]
  flightNumber int
  airlineCode varchar
}
Ref: flight.flightId > scheduledFlight.flightId


Table scheduledFlight {
  scheduledFlightId varchar [primary key]
  flightId varchar
  flightNumber int
  flightScheduleDate timestamp
  haul varchar
  route varchar[]
  flightStatusPublic varchar
  airlineCode varchar
}

Table codeShareRelation {
  codeShareId varchar [primary key]
  flightId varchar
  relatedFlightId varchar
}



Table flightRelation {
  flightRelationId int [primary key]
  scheduledFlightId varchar
  relatedScheduledFlightId varchar
  relationType varchar
}





Table flightLeg {
  flightLegId varchar [primary key]
  scheduledFlightId varchar
  legStatus varchar
  passengerCustomStatus varchar
  serviceType varchar
  serviceTypeName varchar
  restricted varchar
  scheduledFlightDuration varchar
  completionPercentage int
  timeZoneDifference varchar
  aircraftCode varchar
  departureAerogareCode varchar
  departureGateNumber varchar
  departureParkingPosition varchar
  departureParkingPositionCustomStatus varchar
  departureParkingPositionType varchar
  departurePierCode varchar
  departureTerminalCode varchar
  departureAirportCode varchar
  departureActualTime  timestamp
  departureLatestPublishedTime  timestamp
  departureModifiedTime  timestamp
  departureScheduledTime  timestamp
  departureBoardingContactType varchar
  departureBoardingPier varchar
  departureBoardingTerminal varchar
  departureCheckInAerogare varchar
  departureCheckInZone varchar
  departureTerminal varchar
  departurePaxDepartureGate varchar
  departureActualBoardingOpen timestamp
  departureFirstPaxBoarding timestamp
  departureGateCloseTime timestamp
  departurePlannedBoardingTime timestamp
  departureActualTakeOffTime timestamp
  departureEstimatedPublic timestamp
  departureEstimatedTakeOffTime timestamp
  arrivalAerogareCode varchar
  arrivalGateNumber varchar
  arrivalParkingPosition varchar
  arrivalParkingPositionCustomStatus varchar
  arrivalParkingPositionType varchar
  arrivalPierCode varchar
  arrivalTerminalCode varchar
  arrivalAirportCode varchar
  arrivalActualTime  timestamp
  arrivalLatestPublishedTime  timestamp
  arrivalModifiedTime  timestamp
  arrivalScheduledTime  timestamp
  arrivalHall varchar
  arrivalPositionPier varchar
  arrivalPositionTerminal varchar
  arrivalTerminal varchar
  arrivalBaggageBelt varchar
  arrivalDisembarkingAerogare varchar
  arrivalDisembarkingBusQuantity varchar
  arrivalDisembarkingContactType varchar
  arrivalExpectedBagOnBeltTime  timestamp
  arrivalFirstBagOnBeltTime  timestamp
  arrivalLastBagOnBeltTime  timestamp
  arrivalActualTouchDownTime  timestamp
  arrivalEstimated  timestamp
  arrivalEstimatedArrival timestamp
  arrivalEstimatedTouchDownTime timestamp
} 


Table airline {
  code varchar [primary key]
  name varchar
}

Table airport {
  code varchar [primary key]
  name varchar
  latitude float
  longitude float
  cityCode varchar
}

Table city {
  code varchar [primary key]
  name varchar

  countryCode varchar
}

Table country {
  code varchar [primary key]
  name varchar
  euroCountry varchar
  euCountry varchar
  areaCode varchar
}

Table aircraft {
  code varchar [primary key]
  name varchar
  ownerAirlineCode varchar
  physicalPaxConfiguration varchar
  physicalFreightConfiguration varchar
  operationalConfiguration varchar
  cockpitCrewEmployerCode varchar
  cabinCrewEmployerCode varchar
  registration varchar
  saleableConfiguration varchar
  subFleetCodeId varchar
  typeCode varchar
  typeName varchar
  wifiEnabled varchar
}

Table irregularity {
  id varchar [primary key]
  flightLegId varchar
  cancelled varchar
  delayCode varchar
  delayInformation varchar
}

Table delay {
  delayCode varchar [primary key]
  delayReason varchar 
  delayReasonCode varchar
  delayDuration int
}


Ref: flight.flightId > codeShareRelation.flightId
Ref: flight.flightId > codeShareRelation.relatedFlightId

Ref: flightRelation.scheduledFlightId < scheduledFlight.scheduledFlightId
Ref: flightRelation.relatedScheduledFlightId < scheduledFlight.scheduledFlightId

Ref: flightLeg.scheduledFlightId < scheduledFlight.scheduledFlightId
Ref: flightLeg.departureAirportCode < airport.code
Ref: flightLeg.arrivalAirportCode < airport.code
Ref: flightLeg.aircraftCode < aircraft.code
Ref: flightLeg.flightLegId > irregularity.flightLegId
Ref: irregularity.delayCode < delay.delayCode 
Ref: aircraft.cockpitCrewEmployerCode < airline.code
Ref: aircraft.ownerAirlineCode < airline.code
Ref: aircraft.cabinCrewEmployerCode < airline.code

Ref: city.countryCode < country.code

Ref: airport.cityCode < city.code


Ref: airline.code > flight.airlineCode

